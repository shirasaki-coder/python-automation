# CLAUDE.md

このファイルは、Claude Code がこのリポジトリで作業する際のガイドラインです。

## プロジェクト概要

Python を使った自動化スクリプト・ツールを管理するプロジェクトです。

## Git運用ルール

**コードを変更したら、その都度 GitHub にプッシュすること。**

- 変更をコミットする際は、意味のある単位（1つの修正・1つの機能追加など）でコミットを分ける。
- コミットメッセージは変更内容が分かるように簡潔に記述する。
- コミット後は必ず `git push` を実行し、GitHub 上のリモートリポジトリに反映する。
- 作業を中断する場合でも、その時点までの変更はコミット・プッシュしてから終える。
- force push（`git push --force` など）や履歴を書き換える操作は、ユーザーの明示的な指示がない限り行わない。
- コミット前に `git status` / `git diff` で変更内容を確認し、意図しないファイル（認証情報、環境変数ファイルなど）が含まれていないかチェックする。

### 基本フロー

```bash
git add <変更したファイル>
git commit -m "変更内容を説明するメッセージ"
git push
```

## コーディング規約

- 新しい依存関係を追加する場合は、`requirements.txt` を更新する。
- スクリプトには必要最低限のコメントのみを付け、自明な処理には書かない。
- 既存のファイル構成・命名規則に合わせる。

## 開発時の注意

- 破壊的な操作（ファイル削除、既存データの上書きなど）を行う前に、ユーザーに確認する。
- 環境依存の設定（パス、認証情報など）はハードコードせず、環境変数や設定ファイルに切り出す。
